export const setStartDateHelper = (state, { startDate }) => ({
    ...state,
    startDate,
});

export const setAllDates = (state, { startDate, endDate }) => ({
    ...this.state,
    startDate,
    endDate,
});
